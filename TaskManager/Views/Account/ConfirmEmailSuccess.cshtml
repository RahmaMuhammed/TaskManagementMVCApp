@{
    ViewData["Title"] = "Email Confirmed";
}

<div class="text-center mt-5">
    <h2 class="text-success">✔️ Email Confirmed Successfully!</h2>
    <p class="lead mt-3">Your email has been confirmed. You can now <a href="@Url.Action("Login", "Account")">log in</a>.</p>
</div>
